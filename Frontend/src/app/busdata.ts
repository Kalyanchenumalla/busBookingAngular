export interface busData {
    from: string,
    to: string,
    travelDate: string,
    busType: string,
    departure: string,
    arrival: string,
    date: string,
    available: Number,
    fare: Number,
}